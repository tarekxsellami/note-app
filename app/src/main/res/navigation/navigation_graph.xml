<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/welcome">

    <fragment
        android:id="@+id/notesListFragment"
        android:name="com.task.noteapp.ui.noteslist.NotesListFragment"
        android:label="Notes"
        tools:layout="@layout/fragment_notes_list">
        <action
            android:id="@+id/action_notesListFragment_to_addNoteFragment"
            app:destination="@id/addNoteFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />
    </fragment>
    <fragment
        android:id="@+id/addNoteFragment"
        android:name="com.task.noteapp.ui.addnote.AddNoteFragment"
        android:label="Add Notes"
        tools:layout="@layout/fragment_add_note">
        <argument
            android:name="note"
            android:defaultValue="@null"
            app:argType="com.task.noteapp.repository.models.Note"
            app:nullable="true" />
    </fragment>
    <fragment
        android:id="@+id/welcome"
        android:name="com.task.noteapp.ui.onboarding.OnBoardingFragment"
        android:label="Juhuuu! Welcome"
        tools:layout="@layout/fragment_on_boarding">
        <action
            android:id="@+id/action_onBoardingFragment_to_notesListFragment"
            app:destination="@id/notesListFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:launchSingleTop="true"
            app:popUpTo="@+id/navigation_graph"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>